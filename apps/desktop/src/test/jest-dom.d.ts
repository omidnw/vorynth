/**
 * Global type augmentation for @testing-library/jest-dom matchers
 * (toBeInTheDocument, toHaveTextContent, …) in Vitest component tests.
 *
 * Lives as a reference-only declaration — no runtime import here (the setup
 * file handles runtime registration).
 */
/// <reference types="@testing-library/jest-dom" />
